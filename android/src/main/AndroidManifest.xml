<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by Activity.registerScreenCaptureCallback. Declared here so it merges into host
         apps; versions that do not define it ignore it. -->
    <uses-permission android:name="android.permission.DETECT_SCREEN_CAPTURE" />

</manifest>
